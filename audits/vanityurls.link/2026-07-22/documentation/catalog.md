# Documentation Catalog

This catalog is navigation metadata, not audit evidence. Cite the original document and section for every finding.

| ID | Original locator | Cached text locator | Format | Type and topics | Relevant reviewers | Summary (target 75–100 words; max 120) | Limits |
|---|---|---|---|---|---|---|---|
| DOC-001 | `website-docs:content/docs/_index.en.md` | `not-retained` | Markdown | Documentation index; setup, CLI, customization, reference, contribution | product-value, project-health, maintenance-cost | English documentation landing page for readers navigating vanityURLs. It routes from prerequisites and Quickstart through the v8s.link demo, local and editing command-line tools, post-deployment customization, branding, public and legal pages, access control, analytics, policy, network protection, and exact-behavior references. It also points to repository layout, custom overrides, public and status pages, upgrades, and the separate brand site for Hugo-site contributor documentation. The page serves as an orientation map rather than an implementation procedure for operators and contributors. | Index only; it contains no complete setup sequence, configuration values, command syntax, or troubleshooting procedure. |
| DOC-002 | `website-docs:content/docs/_index.fr.md` | `not-retained` | Markdown | French documentation index; setup, CLI, customization, reference, contribution | product-value, project-health, maintenance-cost | French-language documentation landing page for navigating vanityURLs. It links readers to setup prerequisites, the quickstart path, the v8s.link demonstration instance, the command-line helper and `lnk`, and post-deployment customization subjects including branding, public pages, access control, analytics, policy, and network protection. It also routes to exact-behavior references for repository layout, custom overrides, public and status pages, and upgrades, while directing Hugo-site contributors to the separate French brand site. Its scope is orientation across the documentation set. | Index only; it does not supply full procedures, command details, configuration examples, or runtime specifications. |
| DOC-003 | `website-docs:content/docs/command-line-interface/_index.en.md` | `not-retained` | Markdown | CLI section index; tool selection, read-only access, editing, maintenance | code-quality, maintenance-cost, project-health | English section index that helps repository users choose among three local command-line tools by job. It distinguishes the read-only `v8s` helper for opening an existing short link, the `lnk` editing CLI for changing links, schedules, or source policy under `custom/`, and `v8s-fix` for maintenance changes to `custom/public/` after `npm run doctor` recommends a fix. Its scope is tool selection before deeper instructions, with direct links to each tool page. It does not present installation, syntax, or behavior details. | Selection table only; prerequisites, commands, safeguards, write behavior, and troubleshooting reside on linked pages. |
| DOC-004 | `website-docs:content/docs/command-line-interface/_index.fr.md` | `not-retained` | Markdown | French CLI section index; tool selection, read-only access, editing, maintenance | code-quality, maintenance-cost, project-health | French-language section index for choosing among vanityURLs command-line tools. It separates the read-only local helper, used to open an existing short link from a terminal, from the `lnk` CLI, used to modify links, schedules, and source policy in `custom/`. It also identifies `v8s-fix` as the maintenance helper for `custom/public/` when `npm run doctor` recommends a correction. The page is a compact routing aid that links to the three detailed guides rather than documenting their operation itself. | Selection table only; it omits prerequisites, installation steps, command syntax, write behavior, and repair safeguards. |
| DOC-005 | `website-docs:content/docs/command-line-interface/lnk.en.md` | `not-retained` | Markdown | CLI operator guide; links, schedules, policy, Git publishing, environment overrides | code-quality, maintenance-cost, security-privacy, project-health | English operator and maintainer guide for the Node-based `lnk` CLI used when changes should enter Git history and deploy through the Worker workflow. It covers Node.js 20, npm, and Git prerequisites; adding, listing, replacing, tagging, and generating slugs; lifecycle states and metadata; 3.x-compatible schedule commands; source-policy commands; JSON or table output; environment overrides; dry runs; and automated check, stage, commit, and push behavior. It distinguishes `lnk` from the read-only helper and explains broader `local-publish` commit-message selection. | Covers repository CLI behavior, not Worker request handling; schedule writes target the legacy compatibility file during 3.x. |
| DOC-006 | `website-docs:content/docs/command-line-interface/lnk.fr.md` | `not-retained` | Markdown | French CLI operator guide; links, schedules, policy, Git publishing, environment overrides | code-quality, maintenance-cost, security-privacy, project-health | French-language operator and maintainer guide for the Node-based `lnk` CLI, intended for changes that enter Git history and deploy through the normal Worker workflow. It documents prerequisites, slug creation and replacement, generated and tag-specific slug lengths, lifecycle states, metadata, list formats, 3.x-compatible schedules, source-policy commands, environment overrides, dry runs, and the validation, staging, commit, and push sequence. It separates this editing workflow from the read-only helper and describes commit-message selection for broader local publication in Git. | Limited to repository CLI operations; it does not document Worker resolution internals, and schedule writes use the legacy 3.x compatibility file. |
| DOC-007 | `website-docs:content/docs/command-line-interface/local-helper.en.md` | `not-retained` | Markdown | Local-helper guide; read-only registry access, installation, configuration, supported shells | code-quality, maintenance-cost, security-privacy | English operator guide to the read-only `v8s` terminal helper for opening known redirects without first navigating through a browser. It explains accepted lifecycle states and web URL schemes, exact-slug validation, registry sources such as `build/v8s.json` and the user configuration file `.v8s.json`, prerequisites including `jq` and a POSIX-compatible shell, workstation installation through `npm run local-install`, local configuration storage, the `V8S_REGISTRY` override, and list, open, print, and path commands. It explicitly distinguishes the helper from the editing and publishing `lnk` CLI. | The helper cannot create, edit, commit, or push links; Fish and PowerShell cannot directly source its shell script without compatibility support. |
| DOC-008 | `website-docs:content/docs/command-line-interface/local-helper.fr.md` | `not-retained` | Markdown | French local-helper guide; read-only registry access, installation, configuration, supported shells | code-quality, maintenance-cost, security-privacy | French-language operator guide to the read-only `v8s` terminal helper for opening known redirects. It describes exact-slug lookup, the restriction to active `permanent` and `ephemeral` links with HTTP or HTTPS targets, registry files such as `build/v8s.json` and the user configuration file `.v8s.json`, prerequisites including `jq` and a POSIX-compatible shell, workstation installation, settings in `custom/v8s-local-config.json`, the `V8S_REGISTRY` override, and commands for listing, opening, printing, and locating the registry. It contrasts this helper with the editing `lnk` CLI for local repository changes. | It does not create or publish links; shells such as Fish and PowerShell require a compatibility layer to source `scripts/v8s.sh`. |
| DOC-009 | `website-docs:content/docs/command-line-interface/v8s-fix.en.md` | `not-retained` | Markdown | Maintenance guide; doctor-reported drift, public assets, copied pages, override records | maintenance-cost, code-quality, project-health | English maintenance guide for using `v8s-fix` after an upgrade when `npm run doctor` reports repairable drift under `custom/public/`. It presents a doctor-first and dry-run workflow, Git diff review, and fix groups for stale shared assets, head references, product pages, copied languages, branding, all non-destructive repairs, or full public-page recreation. It explains that broad `--public` handling can replace local edits, shows how intentional differences can be recorded narrowly in `custom/v8s-custom-overrides.json`, and relates repairs to the update and upgrade process. | Restricted to `custom/public/` maintenance; it does not replace human review of intentional custom pages or redesign decisions. |
| DOC-010 | `website-docs:content/docs/command-line-interface/v8s-fix.fr.md` | `not-retained` | Markdown | French maintenance guide; doctor-reported drift, public assets, copied pages, override records | maintenance-cost, code-quality, project-health | French-language maintenance guide for `v8s-fix`, used after an update when `npm run doctor` identifies correctable drift in `custom/public/`. It covers running doctor first, previewing with `--dry-run`, reviewing Git changes, and selecting repair groups for assets, shared head references, product pages, languages, branding, combined safe fixes, or complete public-page recreation. It warns that the broad public repair can replace local edits, documents narrow ignore entries for intentional overrides, and explains how doctor and selected fixes follow product-owned update or upgrade changes. | Covers maintenance drift under `custom/public/`; it does not substitute for review of deliberate instance-owned design or HTML. |
| DOC-011 | `website-docs:content/docs/customize/_index.en.md` | `not-retained` | Markdown | Customization overview; phase-2 work, source ownership, build and deployment flow | architecture, product-value, maintenance-cost, project-health | English phase-2 customization overview for readers whose Quickstart deployment is already working. It frames the next work as refining links, branding, public pages, policy, access control, analytics, and operational settings, and links to a practical starting guide. Its central source-ownership model places the product baseline in `defaults/`, instance choices in `custom/`, and generated output in `build/`. A flow diagram shows both source areas entering `npm run build`, producing Worker assets and runtime JSON for Cloudflare deployment. | Overview only; it does not provide topic-specific configuration values, validation steps, or operational procedures. |
| DOC-012 | `website-docs:content/docs/customize/_index.fr.md` | `not-retained` | Markdown | French customization overview; phase-2 work, source ownership, build and deployment flow | architecture, product-value, maintenance-cost, project-health | French-language phase-2 customization overview for an instance that has completed the quickstart path. It introduces refinement of links, branding, public pages, policy, access control, analytics, and operational settings, with a link to a practical prioritization article. The page distinguishes product defaults in `defaults/`, instance-owned choices in `custom/`, and generated files in `build/`. Its diagram traces those source inputs through `npm run build` to Worker assets, runtime JSON, and Cloudflare deployment, while directing users away from editing generated output. | High-level orientation only; detailed configuration, validation, provider setup, and deployment checks are left to linked pages. |
| DOC-013 | `website-docs:content/docs/customize/access-control.en.md` | `not-retained` | Markdown | Access-control runbook; Cloudflare Access, identity providers, JWT audience, protected operational paths | security-privacy, architecture, business-continuity, contributor-vendor-value | English setup and validation runbook for protecting private dashboard and test paths with Cloudflare Access while leaving public redirects open. It uses the `v8s-config` Terraform repository as the v8s.link reference and supplies manual steps for review, troubleshooting, or migration. Topics include first-time Zero Trust activation, Team domain storage, identity-provider choices, a self-hosted application, localized stats and test destinations, an allow policy, the `CF_ACCESS_AUD` Worker secret, JWT validation flow, legacy redirects, policy testing, signed-out browser checks, and post-deployment verification. | Focuses on Access for operational paths; the complete runtime guard table and broader identity-provider strategy are referenced elsewhere. |
| DOC-014 | `website-docs:content/docs/customize/access-control.fr.md` | `not-retained` | Markdown | French access-control runbook; Cloudflare Access, identity providers, JWT audience, protected operational paths | security-privacy, architecture, business-continuity, contributor-vendor-value | French-language setup and validation runbook for Cloudflare Access protection of private dashboard and test routes, with public redirects kept outside authentication. It points to the `v8s-config` Terraform repository as the v8s.link reference and provides manual steps for existing-zone review, troubleshooting, and migration. It covers Zero Trust activation, the Team domain, provider choices, the self-hosted application, localized stats and QA destinations, maintainer allow policies, storage of `CF_ACCESS_AUD`, JWT checks, legacy route redirects, local validation, signed-out testing, and verification after deployment. | Limited to operational-path Access configuration; broader runtime guards and identity-provider strategy remain in linked references. |
| DOC-015 | `website-docs:content/docs/customize/analytics.en.md` | `not-retained` | Markdown | Analytics setup guide; server-side events, Umami, Fathom, privacy, verification | security-privacy, product-value, expense-exposure, contributor-vendor-value | English setup guide for operators considering server-side analytics without browser tracking JavaScript. It explains asynchronous Worker delivery through `ctx.waitUntil()`, when leaving analytics disabled is acceptable, and when campaigns, QR codes, launches, old links, lookups, or realistic misses may warrant collection. It compares disabled, Umami, Fathom, and temporary dual-provider modes; shows Worker variables and Umami IP-detail choices; separates Fathom collection from management credentials; describes how edge controls affect event visibility and quota; and supplies local smoke and deployed verification steps. | Detailed event fields, provider payloads, IP behavior, and blocked-traffic semantics are deferred to the analytics reference. |
| DOC-016 | `website-docs:content/docs/customize/analytics.fr.md` | `not-retained` | Markdown | French analytics setup guide; server-side events, Umami, Fathom, privacy, verification | security-privacy, product-value, expense-exposure, contributor-vendor-value | French-language setup guide for server-side redirect and page analytics without browser tracking JavaScript. It describes asynchronous Worker delivery, reasons to keep collection disabled or enable it for campaigns, QR codes, launches, old links, lookups, and realistic misses, and choices among Umami, Fathom, or temporary dual collection. It shows the corresponding Worker variables, recommends reduced Umami location detail for privacy-oriented deployments, separates Fathom collection from management API credentials, connects edge blocking to analytics quotas, and gives local smoke-test and deployed-event verification procedures. | It does not enumerate complete event payloads or IP semantics; those details and blocked-traffic behavior are assigned to the reference page. |
| DOC-017 | `website-docs:content/docs/customize/blocklist.en.md` | `not-retained` | Markdown | Policy customization guide; allow and block rules, source replacement, validation, incident review | security-privacy, architecture, business-continuity, project-health | English customization guide for instance-specific trust-and-safety decisions beyond the product policy baseline. It tells operators to review `defaults/v8s-policies.json`, identifies concrete reasons for a local policy, and shows `custom/v8s-policies.json` entries for allowed domains, blocked domains, keywords, and extensions. The custom file replaces rather than merges over the default source. It describes narrow owner-controlled allow rules and their safety boundaries, link validation through `npm run check`, optional generated blocklist updates, and policy review after abuse reports, destination changes, feed updates, or public campaigns. | The page omits the full category, feed, runtime-artifact, and field specification, which is delegated to the policy reference. |
| DOC-018 | `website-docs:content/docs/customize/blocklist.fr.md` | `not-retained` | Markdown | French policy customization guide; allow and block rules, source replacement, validation, incident review | security-privacy, architecture, business-continuity, project-health | French-language customization guide for local trust-and-safety policy beyond vanityURLs product defaults. It directs operators to inspect `defaults/v8s-policies.json`, lists circumstances for creating an override, and demonstrates allowed domains, blocked domains, keywords, and extensions in `custom/v8s-policies.json`. It states that the custom source replaces the default instead of merging with it, explains narrow owner-controlled allow rules and their limits, requires validation with `npm run check`, and identifies abuse reports, major destination changes, feed updates, and public campaigns as occasions for review or optional generated-policy refreshes. | Full category definitions, generated feeds, runtime artifacts, and individual field semantics remain outside this guide. |
| DOC-019 | `website-docs:content/docs/customize/cf-access-not-configured.fr.png` | `not-retained` | PNG | Supporting screenshot; Cloudflare Access fail-closed state, private browser view | security-privacy, product-value | French-associated PNG supporting the Access-control documentation. It is a 1884-by-672 browser-style screenshot of a private v8s.link window with an address bar, Safari-like controls, and a white page displaying the single message “Cloudflare Access is not configured.” Its documentary purpose is to illustrate the fail-closed response shown when a protected operational path is opened without configured Access. The image contains no configuration steps, identity information, path names, policy values, timestamps, or error diagnostics beyond that visible message. | Standalone raster with no searchable text cache or embedded procedural context; its displayed message remains in English. |
| DOC-020 | `website-docs:content/docs/customize/cf-access-not-configured.png` | `not-retained` | PNG | Supporting image asset; nominal Cloudflare Access not-configured state | security-privacy, product-value | PNG asset associated by filename with the English Access-control page. In the available visual renderer, the 1884-by-672 RGBA image appears as an entirely black or transparency-affected canvas, with no legible browser chrome, message, configuration value, path, identity, or timestamp. It therefore supplies no readable procedural content through this inspection. The filename connects the asset to the Cloudflare Access “not configured” state, but the pixels visible through the renderer do not expose additional source text, interface labels, or operational details. | No searchable cache; alpha rendering may obscure intended content, so only the visually exposed canvas and file metadata are described. |
| DOC-021 | `website-docs:content/docs/customize/footer-pages.en.md` | `not-retained` | Markdown | Public-page guide; footer links, legal and trust pages, contacts, custom overrides | security-privacy, product-value, business-continuity | English operator guide to public footer, privacy, terms, Trust and Safety, security, and `security.txt` outputs derived from `custom/v8s-site-config.json`. It explains the public audiences for these pages, how setup can defer full legal pages while retaining abuse and vulnerability contacts, and which generated routes to review. It covers conditional footer links, extension-free English aliases, localized directories, custom HTML override paths in `custom/public/`, contact consistency, and the need to consult the public-page CSP rules before adding inline code, forms, or custom headers. | It maps generated and override surfaces but does not provide legal text or the full custom-page CSP specification. |
| DOC-022 | `website-docs:content/docs/customize/footer-pages.fr.md` | `not-retained` | Markdown | French public-page guide; footer links, legal and trust pages, contacts, custom overrides | security-privacy, product-value, business-continuity | French-language operator guide to footer links and generated privacy, conditions, Trust and Safety, security, and `security.txt` pages. It describes how operator values in `custom/v8s-site-config.json` give visitors, reporters, registrars, and security researchers a public contact path. It explains deferring complete legal pages while retaining abuse and vulnerability reporting, lists generated outputs, discusses language-specific footer links, and maps fully custom privacy, terms, abuse, and security HTML under `custom/public/`. It also directs custom scripts, styles, forms, and headers to separate CSP guidance. | It does not supply legal wording or reproduce the complete security model for custom public pages. |
| DOC-023 | `website-docs:content/docs/customize/jurisdiction.en.md` | `not-retained` | Markdown | Setup decision guide; operator identity, jurisdiction, governing law, public contacts | security-privacy, business-continuity, product-value | English phase-1 decision guide for operator, jurisdiction, governing-law, and trust-contact values entered before or during `npm run setup`. It explains operator naming, optional review of public contact emails, deriving role-based addresses from the short or operator domain, and separate abuse, security, general, and privacy contacts. For full legal pages, it covers jurisdiction, governing law, and last-reviewed date. It also frames the Trust and Safety response window as a human expectation, explains multilingual reuse of custom wording, and identifies public-commitment changes that warrant rerunning setup. | Explicitly not legal advice; generated paths, footer aliases, and page overrides are handled by the separate footer-pages guide. |
| DOC-024 | `website-docs:content/docs/customize/jurisdiction.fr.md` | `not-retained` | Markdown | French setup decision guide; operator identity, jurisdiction, governing law, public contacts | security-privacy, business-continuity, product-value | French-language phase-1 guide for choosing operator, jurisdiction, governing-law, and public contact values around `npm run setup`. It covers the responsible entity’s name, review of role-based contact addresses, use of either the short domain or an operator domain, and distinct abuse, security, general, and privacy contacts. When complete legal pages are enabled, it explains jurisdiction, governing law, and the review date. It also treats the Trust and Safety response window as a realistic human expectation and lists meaningful public-content changes that call for another setup run. | The page states that it is not legal advice; output paths, footer links, aliases, and custom-page mechanics are documented elsewhere. |
| DOC-025 | `website-docs:content/docs/customize/network-protection.en.md` | `not-retained` | Markdown | Cloudflare operations runbook; DNS, TLS, WAF, rate limits, bots, network, caching | security-privacy, architecture, scalability, business-continuity, contributor-vendor-value, expense-exposure | English Cloudflare operations runbook for controls placed in front of the vanityURLs Worker. Using the v8s.link Terraform configuration and dashboard checklist, it covers the Worker custom domain, `www`-to-apex redirect, strict TLS baseline, repository-owned headers, free-plan security settings, managed AI-bot controls, crawler exceptions, WAF expressions, short-link and lookup rate limits, URL normalization, managed transforms, network options, and conservative caching. It also identifies Cloudflare features that can inject scripts, rewrite content, expose more visitor data, duplicate repository policy, or bypass vanityURLs workflows. | Dashboard labels, plan entitlements, and managed crawler classifications can change; the page directs readers to current Cloudflare changelogs and raw captures. |
| DOC-026 | `website-docs:content/docs/customize/network-protection.fr.md` | `not-retained` | Markdown | French Cloudflare operations runbook; DNS, TLS, WAF, rate limits, bots, network, caching | security-privacy, architecture, scalability, business-continuity, contributor-vendor-value, expense-exposure | French-language Cloudflare runbook for reducing unwanted traffic before requests reach the vanityURLs Worker. It uses the v8s.link Terraform configuration and a dashboard checklist to cover the Worker apex domain, proxied `www` redirect, TLS settings, repository-owned headers, free-plan security controls, AI-bot and crawler handling, custom WAF rules, short-link and lookup rate limits, URL normalization, managed transforms, network settings, and conservative cache behavior. It also names controls that can inject scripts, rewrite public content, add visitor data, duplicate repository policy, or bypass registry behavior. | Cloudflare menus, product labels, plan limits, and managed bot classifications are time-sensitive and require comparison with current vendor documentation. |
| DOC-027 | `website-docs:content/docs/reference/_index.en.md` | `not-retained` | Markdown | Reference index; files, runtime, branding, internationalization, security, operations | architecture, code-quality, maintenance-cost, project-health, security-privacy | English reference-section index for readers who already have a running instance and need the exact shape or behavior of a file, runtime artifact, default asset, or operational surface. Its table routes to analytics, branding, the operator cheatsheet, Cloudflare products, configuration files, custom overrides, glossary, internationalization, link format, policy and blocklist, public pages, repository layout, runtime security, the security model, schedules, timezones, and upgrades. Each entry supplies a short statement of the questions or artifacts addressed by the linked page. | Navigation page only; it contains no complete file schemas, runtime algorithms, security controls, or upgrade procedure. |
| DOC-028 | `website-docs:content/docs/reference/_index.fr.md` | `not-retained` | Markdown | French reference index; files, runtime, branding, internationalization, security, operations | architecture, code-quality, maintenance-cost, project-health, security-privacy | French-language reference index for users of an existing vanityURLs instance seeking exact information about files, runtime artifacts, default assets, or operational surfaces. It links to analytics, branding, the operator aide-mémoire, Cloudflare products, configuration files, custom overrides, terminology, internationalization, link format, policy and blocklist behavior, public pages, repository layout, runtime security, the security model, scheduled links, timezones, and upgrades. The table briefly states the focus of every destination so readers can choose the appropriate detailed reference. | Routing table only; detailed schemas, payloads, runtime behavior, control mappings, and operational steps remain on linked pages. |
| DOC-029 | `website-docs:content/docs/reference/analytics.en.md` | `not-retained` | Markdown | Technical analytics reference; configuration, events, payloads, IP handling, blocked traffic | security-privacy, architecture, scalability, expense-exposure, contributor-vendor-value, product-value | English technical reference for analytics executed inside the Cloudflare Worker without browser JavaScript, cookies, or visitor accounts. It documents asynchronous sending, Worker variables and secrets for Umami and Fathom, event names for pageviews, redirects, misses, lookups, and bots, localized lookup routes, and the boundary between Worker events and traffic blocked at Cloudflare. It enumerates Umami and Fathom payload fields, distinguishes collection from management APIs, explains Umami full, truncated, and omitted IP modes, and notes the handling of known bot user agents. | Vendor limits depend on the account and product; current plan documentation must be consulted before high-volume collection. |
| DOC-030 | `website-docs:content/docs/reference/analytics.fr.md` | `not-retained` | Markdown | French technical analytics reference; configuration, events, payloads, IP handling, blocked traffic | security-privacy, architecture, scalability, expense-exposure, contributor-vendor-value, product-value | French-language technical reference for vanityURLs analytics running in the Cloudflare Worker without browser tracking JavaScript, cookies, or visitor accounts. It covers asynchronous delivery, Umami and Fathom variables and credentials, pageview, redirect, miss, lookup, and bot events, localized consultation routes, and the absence of vanityURLs events for traffic blocked before the Worker. It lists the two providers’ request and event fields, separates runtime collection from management APIs, describes Cloudflare analytics surfaces, and defines Umami IP forwarding modes plus Fathom’s user-agent handling. | Provider quotas and plan behavior are account-specific and time-sensitive; the page directs readers to current vendor documentation. |
| DOC-031 | `website-docs:content/docs/reference/brand.en.md` | `not-retained` | Markdown | Branding reference; setup questions, wordmarks, slogans, asset overrides, build behavior | product-value, maintenance-cost, contributor-vendor-value | English instance-branding reference for operators configuring generated public pages. It explains that visual standards live on the dedicated brand site while wordmark portions, localized slogans, and the operator domain live in `custom/v8s-site-config.json` and are applied during the build. It documents setup questions, English-first slogan fallback, operator-name links, asset overrides under `custom/public/`, localized badges, and the default flow from `defaults/public/` to `build/`. It also covers optional copying of full templates, language pruning, repeated setup behavior, and `--force` protection. | Full logo, color, typography, badge, and product-interface standards are external to this page on the dedicated brand site. |
| DOC-032 | `website-docs:content/docs/reference/brand.fr.md` | `not-retained` | Markdown | French branding reference; setup questions, wordmarks, slogans, asset overrides, build behavior | product-value, maintenance-cost, contributor-vendor-value | French-language instance-branding reference for generated vanityURLs pages. It directs visual-standard questions to the dedicated French brand site while documenting the wordmark, localized slogans, operator-domain link, and values stored in `custom/v8s-site-config.json`. It lists setup questions and phase-1 choices, identifies instance-owned public asset and localized badge paths, and describes applying branding while copying `defaults/public/` into `build/`. It also explains optional full-template copies, supported-language pruning, repeatable setup behavior, and refusal to replace unmanaged `custom/public/` content without `--force` for instance operators. | Detailed color, typography, logo, badge, product-interface, and downloadable-asset standards remain on the separate brand site. |
| DOC-033 | `website-docs:content/docs/reference/cheatsheet.en.md` | `not-retained` | Markdown | Operator cheatsheet; workflows, repositories, core files, links, schedules, troubleshooting | project-health, maintenance-cost, code-quality, architecture | English compact operator reference intended for readers who already know vanityURLs and need the shortest route to a routine action, on screen or in a browser-generated PDF. Its cards summarize dependency installation, local startup, validation, deployment, repository roles, core instance and product files, link-row fields, schedule syntax, redirect outcomes, safe customization boundaries, and common troubleshooting checks. A final comparison distinguishes `defaults/` from `custom/`, exact slugs from splat namespaces, and generated output from editable sources, with brief operational reminders. | Deliberately condensed; it omits full setup, command options, file schemas, policy rationale, and detailed diagnostic procedures. |
| DOC-034 | `website-docs:content/docs/reference/cheatsheet.fr.md` | `not-retained` | Searchable Markdown (French) | Operator quick reference; workflows, repositories, files, links, schedules, customization, troubleshooting | architecture, code-quality, maintenance-cost, product-value, project-health | French quick reference for operators already familiar with vanityURLs, designed for on-screen use and browser PDF export. It summarizes the current daily install, local-run, validation, and deployment workflow; repository responsibilities; source and generated files; link fields and lifecycle behavior; exact-link schedules; customization boundaries; and common troubleshooting checks. It also contrasts product defaults with instance overrides and exact links with splat namespaces. The page assumes prior product knowledge and serves as a compact action lookup rather than a first-time setup guide. | French only; compact reference without complete schemas, command options, or detailed setup procedures. |
| DOC-035 | `website-docs:content/docs/reference/cloudflare-products.en.md` | `not-retained` | Searchable Markdown (English) | Platform reference; Cloudflare DNS, Workers, Access, TLS, WAF, rate limiting, bots, rules, security events | architecture, security-privacy, expense-exposure, scalability, maintenance-cost | English reference for operators and maintainers identifying the Cloudflare products used by the vanityURLs operating baseline. It maps DNS, Workers, Access, and SSL/TLS to redirector functions, then lists edge protections applied before traffic reaches the Worker, including WAF, rate limiting, DDoS protection, bot controls, AI crawler controls, rules, and security events. Because Cloudflare is described as an evergreen software-service platform, the page also explains the structured dashboard capture used by maintainers to track changing APIs, labels, features, and navigation over time. | Identifies product roles but does not provide dashboard setup steps, plan requirements, pricing, or availability details. |
| DOC-036 | `website-docs:content/docs/reference/cloudflare-products.fr.md` | `not-retained` | Searchable Markdown (French) | Platform reference; Cloudflare DNS, Workers, Access, TLS, WAF, rate limiting, bots, rules, security events | architecture, security-privacy, expense-exposure, scalability, maintenance-cost | French reference for operators and maintainers identifying the Cloudflare products used by the vanityURLs operating baseline. It maps DNS, Workers, Access, and SSL/TLS to redirector functions, then lists edge protections applied before traffic reaches the Worker, including WAF, rate limiting, DDoS protection, bot controls, AI crawler controls, rules, and security events. Because Cloudflare is described as an evolving logiciel-service platform, the page also explains the structured dashboard capture used by maintainers to compare interface changes and deliberately update documentation over time. | French counterpart; identifies product roles but omits dashboard setup steps, plan requirements, pricing, and availability details. |
| DOC-037 | `website-docs:content/docs/reference/configuration-files.en.md` | `not-retained` | Searchable Markdown (English) | Configuration reference; source, custom, workstation, generated files, schemas, ownership, build order | architecture, code-quality, maintenance-cost, security-privacy | Current English reference for operators and maintainers locating vanityURLs configuration contracts. It separates product defaults, instance-owned choices, workstation settings, and generated runtime artifacts; records file formats, schema shapes, owners, and generators; and describes the site configuration merge, public override order, build inputs, and deployed outputs. Its temporal scope includes schema `1.0`, runtime registry schema `3.1`, deprecated 3.x schedule compatibility, and legacy blocklist filenames. The page warns against editing generated files and states that the exact implementation contract remains the repository files and scripts. | Describes contracts and examples but does not reproduce every field validator or implementation detail from the code repository. |
| DOC-038 | `website-docs:content/docs/reference/configuration-files.fr.md` | `not-retained` | Searchable Markdown (French) | Configuration reference; source, custom, workstation, generated files, schemas, ownership, build order | architecture, code-quality, maintenance-cost, security-privacy | Current French reference for operators and maintainers locating vanityURLs configuration contracts. It separates product defaults, instance-owned choices, workstation settings, and generated runtime artifacts; records file formats, schema shapes, owners, and generators; and describes the site configuration merge, public override order, build inputs, and deployed outputs. Its temporal scope includes schema `1.0`, runtime registry schema `3.1`, deprecated 3.x schedule compatibility, and legacy blocklist filenames. The page warns against editing generated files and states that the exact implementation contract remains the repository files and scripts. | French counterpart; does not reproduce every field validator or implementation detail from the code repository. |
| DOC-039 | `website-docs:content/docs/reference/custom-overrides.en.md` | `not-retained` | Searchable Markdown (English) | Operator reference; instance overlays, upgrade boundaries, public assets, maintenance ignores, headers and CSP | architecture, maintenance-cost, code-quality, contributor-vendor-value, security-privacy | English reference for operators managing instance-specific changes under `custom/`. It explains the current boundary between the product baseline in `defaults/` and instance overlays intended to survive upstream updates. A file map connects links, legacy schedules, policy, site and local configuration, maintenance exceptions, public pages, assets, and headers to their purposes. The page also distinguishes deliberate `npm run doctor` exceptions from repairable drift and summarizes when custom public headers or CSP changes are appropriate. Full build ordering and individual format details are delegated to linked references. | Does not define complete file schemas, the full build pipeline, or individual customization procedures. |
| DOC-040 | `website-docs:content/docs/reference/custom-overrides.fr.md` | `not-retained` | Searchable Markdown (French) | Operator reference; instance overlays, upgrade boundaries, public assets, maintenance ignores, headers and CSP | architecture, maintenance-cost, code-quality, contributor-vendor-value, security-privacy | French reference for operators managing instance-specific changes under `custom/`. It explains the current boundary between the product baseline in `defaults/` and instance overlays intended to survive upstream updates. A file map connects links, legacy schedules, policy, site and local configuration, maintenance exceptions, public pages, assets, and headers to their purposes. The page also distinguishes deliberate `npm run doctor` exceptions from repairable drift and summarizes when custom public headers or CSP changes are appropriate. Full build ordering and individual format details are delegated to linked references. | French counterpart; does not define complete file schemas, the full build pipeline, or individual customization procedures. |
| DOC-041 | `website-docs:content/docs/reference/glossary.en.md` | `not-retained` | Searchable Markdown (English) | Glossary landing page; website terminology and abbreviations | contributor-vendor-value, product-value, maintenance-cost | English glossary landing page for readers of the vanityURLs website. Its stated purpose is to define common product terms and abbreviations used across the documentation, blog, and public pages. The Markdown body contains one sentence describing that scope and invokes a `glossary` shortcode for the displayed glossary content. It has no dated scope, procedural workflow, configuration guidance, architectural explanation, or term definitions directly embedded in this source file; the visible entries depend on content supplied when the shortcode is rendered. | Source contains no individual glossary entries, definitions, ownership metadata, or update history. |
| DOC-042 | `website-docs:content/docs/reference/glossary.fr.md` | `not-retained` | Searchable Markdown (French) | Glossary landing page; website terminology and abbreviations | contributor-vendor-value, product-value, maintenance-cost | French glossary landing page for readers of the vanityURLs website. Its stated purpose is to define common product terms and abbreviations used across the documentation, blog, and public pages. The Markdown body contains one sentence describing that scope and invokes a `glossary` shortcode for the displayed glossary content. It has no dated scope, procedural workflow, configuration guidance, architectural explanation, or term definitions directly embedded in this source file; the visible entries depend on content supplied when the shortcode is rendered. | Source contains no individual glossary entries, definitions, ownership metadata, or update history. |
| DOC-043 | `website-docs:content/docs/reference/i18n.en.md` | `not-retained` | Searchable Markdown (English) | Internationalization reference; language detection, localized pages, configuration, build pruning, badges | product-value, architecture, maintenance-cost, security-privacy | English operator reference for the current vanityURLs internationalization model. It defines i18n, lists English, French, Spanish, Italian, and German support, and explains browser-language detection with English fallback. It inventories localized public and lifecycle pages, shows the `custom/v8s-site-config.json` language settings written by the installer, and describes how the build overlays custom pages and removes unsupported language directories. It also documents localized override paths and light and dark redirected badges. The page covers shipped page localization, not translation production or a broader locale-negotiation specification. | Limited to five listed languages and public-page assets; no translation workflow or detailed browser negotiation algorithm. |
| DOC-044 | `website-docs:content/docs/reference/i18n.fr.md` | `not-retained` | Searchable Markdown (French) | Internationalization reference; language detection, localized pages, configuration, build pruning, badges | product-value, architecture, maintenance-cost, security-privacy | French operator reference for the current vanityURLs internationalization model. It defines i18n, lists English, French, Spanish, Italian, and German support, and explains browser-language detection with English fallback. It inventories localized public and lifecycle pages, shows the `custom/v8s-site-config.json` language settings written by the installer, and describes how the build overlays custom pages and removes unsupported language directories. It also documents localized override paths and light and dark redirected badges. The page covers shipped page localization, not translation production or a broader locale-negotiation specification. | French counterpart; limited to five listed languages and public-page assets, with no translation workflow or detailed negotiation algorithm. |
| DOC-045 | `website-docs:content/docs/reference/link-format.en.md` | `not-retained` | Searchable Markdown (English) | Data-format reference; source registry, exact and splat links, schedules, states, metadata, slug validation | architecture, code-quality, security-privacy, product-value | English reference for operators and maintainers editing the current human-authored `v8s-links.txt` source registry. It defines the pipe-delimited fields, required and optional metadata, exact links, inline schedules, splat namespaces, lifecycle states, expiration behavior, slug character rules, and reserved top-level paths. It explains that the Worker does not read this source at request time; the build compiles it into `build/v8s.json`. Examples show exact and splat entries sharing a base slug. The generated tree schema and implementation validators are outside this file’s detailed scope. | Does not document the complete generated registry schema, build implementation, or policy-validation internals. |
| DOC-046 | `website-docs:content/docs/reference/link-format.fr.md` | `not-retained` | Searchable Markdown (French) | Data-format reference; source registry, exact and splat links, schedules, states, metadata, slug validation | architecture, code-quality, security-privacy, product-value | French reference for operators and maintainers editing the current human-authored `v8s-links.txt` source registry. It defines the pipe-delimited fields, required and optional metadata, exact links, inline schedules, splat namespaces, lifecycle states, expiration behavior, slug character rules, and reserved top-level paths. It explains that the Worker does not read this source at request time; the build compiles it into `build/v8s.json`. Examples show exact and splat entries sharing a base slug. The generated tree schema and implementation validators are outside this file’s detailed scope. | French counterpart; does not document the complete generated registry schema, build implementation, or policy-validation internals. |
| DOC-047 | `website-docs:content/docs/reference/policy-blocklist.en.md` | `not-retained` | Searchable Markdown (English) | Trust-and-safety reference; policy selection, categories, generated feeds, allow/block behavior, runtime artifact | security-privacy, architecture, business-continuity, revenue-risk | English reference for operators and security reviewers distinguishing editable source policy from the generated runtime blocklist artifact. It documents current source selection, including replacement rather than merging when `custom/v8s-policies.json` exists; category and severity metadata; generated-feed expectations and false-positive handling; and the behavior of allowed domains, blocked domains, keywords, and extensions. It identifies `build/v8s-blocklist.json` as Worker-consumed output blocked from direct public access and notes legacy source filenames retained for migration compatibility. Detailed runtime protections and the operator change workflow are delegated elsewhere. | Does not provide the complete operator procedure, feed contents, or built-in runtime-control implementation. |
| DOC-048 | `website-docs:content/docs/reference/policy-blocklist.fr.md` | `not-retained` | Searchable Markdown (French) | Trust-and-safety reference; policy selection, categories, generated feeds, allow/block behavior, runtime artifact | security-privacy, architecture, business-continuity, revenue-risk | French reference for operators and security reviewers distinguishing editable source policy from the generated runtime blocklist artifact. It documents current source selection, including replacement rather than merging when `custom/v8s-policies.json` exists; category and severity metadata; generated-feed expectations and false-positive handling; and the behavior of allowed domains, blocked domains, keywords, and extensions. It identifies `build/v8s-blocklist.json` as Worker-consumed output blocked from direct public access and notes legacy source filenames retained for migration compatibility. Detailed runtime protections and the operator change workflow are delegated elsewhere. | French counterpart; does not provide the complete operator procedure, feed contents, or built-in runtime-control implementation. |
| DOC-049 | `website-docs:content/docs/reference/public-pages.en.md` | `not-retained` | Searchable Markdown (English) | Customization and security reference; public overrides, status pages, CSP sandbox, doctor ignores, SRI | security-privacy, product-value, architecture, maintenance-cost | English operator reference for current public-page, asset, and status-page overrides under `custom/public/`. It maps branding, legal, localized, lookup, dashboard, and header paths; defines status files and HTTP outcomes; and identifies the two placeholders available only to custom `404.html`. It explains deliberate `npm run doctor` ignores, the generated custom-asset manifest, and the sandboxed CSP applied to custom HTML without `allow-same-origin`. It also records build-time SRI ordering and failure modes. Canonical CSP details and broader configuration mapping remain in linked references. | Does not include complete HTML templates, a full CSP implementation, or general branding and access-control procedures. |
| DOC-050 | `website-docs:content/docs/reference/public-pages.fr.md` | `not-retained` | Searchable Markdown (French) | Customization and security reference; public overrides, status pages, CSP sandbox, doctor ignores, SRI | security-privacy, product-value, architecture, maintenance-cost | French operator reference for current public-page, asset, and status-page overrides under `custom/public/`. It maps branding, legal, localized, lookup, dashboard, and header paths; defines status files and HTTP outcomes; and identifies the two placeholders available only to custom `404.html`. It explains deliberate `npm run doctor` ignores, the generated custom-asset manifest, and the sandboxed CSP applied to custom HTML without `allow-same-origin`. It also records build-time SRI ordering and failure modes. Canonical CSP details and broader configuration mapping remain in linked references. | French counterpart; does not include complete HTML templates, a full CSP implementation, or general branding and access-control procedures. |
| DOC-051 | `website-docs:content/docs/reference/repository-layout.en.md` | `not-retained` | Searchable Markdown (English) | Architecture reference; repository ownership, defaults, custom files, tooling, generated outputs, upgrades | architecture, code-quality, maintenance-cost, contributor-vendor-value, business-continuity | Current English architecture reference for operators, maintainers, and contributors navigating a vanityURLs instance repository. It presents the public `v8s.link` tree and separates product-managed defaults and scripts, instance-owned custom files and Wrangler settings, canonical Worker source, and generated `build/` and `src/` outputs. It explains link and policy fallback or replacement rules, product asset shadow overrides, doctor exceptions, upgrade ownership, runtime registries, custom-asset and release manifests, and cleanup behavior. The page describes file responsibilities and generated relationships rather than the internal logic or full schemas of each component. | Covers repository organization, not complete source-code behavior, command options, or every artifact schema. |
| DOC-052 | `website-docs:content/docs/reference/repository-layout.fr.md` | `not-retained` | Searchable Markdown (French) | Architecture reference; repository ownership, defaults, custom files, tooling, generated outputs, upgrades | architecture, code-quality, maintenance-cost, contributor-vendor-value, business-continuity | Current French architecture reference for operators, maintainers, and contributors navigating a vanityURLs instance repository. It presents the public `v8s.link` tree and separates product-managed defaults and scripts, instance-owned custom files and Wrangler settings, canonical Worker source, and generated `build/` and `src/` outputs. It explains link and policy fallback or replacement rules, product asset shadow overrides, doctor exceptions, upgrade ownership, runtime registries, custom-asset and release manifests, and cleanup behavior. The page describes file responsibilities and generated relationships rather than the internal logic or full schemas of each component. | French counterpart; covers organization rather than complete source behavior, command options, or every artifact schema. |
| DOC-053 | `website-docs:content/docs/reference/runtime-registry.en.md` | `not-retained` | Searchable Markdown (English) | Runtime data-contract reference; schema 3.1 tree, exact and splat resolution, tooling, validation | architecture, code-quality, scalability, security-privacy | English reference for maintainers reviewing the generated `build/v8s.json` registry consumed by the Worker. It distinguishes the human-edited flat source from the tree-first runtime representation and records the cleanup direction of retaining a single canonical runtime shape. Its current contract is schema `3.1`, with generation metadata, default state, routing, and a nested `tree` whose nodes may contain exact and splat links. It explains how dashboards and maintenance tools flatten the tree and lists build checks for structure, targets, schedules, splat placeholders, and blocklist violations. | Does not define the generator and validator implementation; the code repository remains the implementation contract. |
| DOC-054 | `website-docs:content/docs/reference/runtime-registry.fr.md` | `not-retained` | Searchable Markdown (French) | Runtime data-contract reference; schema 3.1 tree, exact and splat resolution, tooling, validation | architecture, code-quality, scalability, security-privacy | French reference for maintainers reviewing the generated `build/v8s.json` registry consumed by the Worker. It distinguishes the human-edited flat source from the tree-first runtime representation and records the cleanup direction of retaining a single canonical runtime shape. Its current contract is schema `3.1`, with generation metadata, default state, routing, and a nested `tree` whose nodes may contain exact and splat links. It explains how dashboards and maintenance tools flatten the tree and lists build checks for structure, targets, schedules, splat placeholders, and blocklist violations. | French counterpart; does not define the generator and validator implementation, which remains in the code repository. |
| DOC-055 | `website-docs:content/docs/reference/runtime-security.en.md` | `not-retained` | Searchable Markdown (English) | Runtime security reference; request resolution, target controls, build guardrails, CSP, private assets, edge controls | security-privacy, architecture, scalability, business-continuity, code-quality | English compact reference for maintainers, operators, and security reviewers preserving the current Worker security controls. It describes the narrow request and resolution path, accepted methods and lookup exceptions, redirect-target checks, scanner handling, lifecycle routing, Access validation, analytics dispatch, and blocked target classes. It also covers build-time validation, generated-source boundaries, response headers, strict product and sandboxed custom CSP profiles, operational file guards, protected paths, CORS treatment for `Origin: null`, and division of responsibilities with Cloudflare edge controls. Design rationale and detailed operator workflows are linked rather than repeated. | Does not replace the linked design rationale, network-protection setup, Access setup, or implementation-level security review. |
| DOC-056 | `website-docs:content/docs/reference/runtime-security.fr.md` | `not-retained` | Searchable Markdown (French) | Runtime security reference; request resolution, target controls, build guardrails, CSP, private assets, edge controls | security-privacy, architecture, scalability, business-continuity, code-quality | French compact reference for maintainers, operators, and security reviewers preserving the current Worker security controls. It describes the narrow request and resolution path, accepted methods and lookup exceptions, redirect-target checks, scanner handling, lifecycle routing, Access validation, analytics dispatch, and blocked target classes. It also covers build-time validation, generated-source boundaries, response headers, strict product and sandboxed custom CSP profiles, operational file guards, protected paths, CORS treatment for `Origin: null`, and division of responsibilities with Cloudflare edge controls. Design rationale and detailed operator workflows are linked rather than repeated. | French counterpart; does not replace the linked rationale, network-protection setup, Access setup, or implementation-level review. |
| DOC-057 | `website-docs:content/docs/reference/schedules.en.md` | `not-retained` | Searchable Markdown (English) | Scheduling reference; exact links, inline directives, fallback targets, rule ordering, legacy compatibility | product-value, architecture, code-quality, business-continuity | English operator reference for time-aware links using inline `@schedule` directives beneath entries in `v8s-links.txt`. It documents the current exact-link-only scope, normal-target fallback, full and compact forms, timezone placement, ordered rule evaluation, and support for overnight windows with inclusive starts and exclusive ends. It also states that `custom/v8s-schedules.json` remains readable during the 3.x series for compatibility with existing instances and the current CLI, while newly hand-authored schedules belong inline. Splat links remain path-driven and outside scheduling support. | Does not cover splat schedules or detailed use-case selection; those are explicitly outside or linked elsewhere. |
| DOC-058 | `website-docs:content/docs/reference/schedules.fr.md` | `not-retained` | Searchable Markdown (French) | Scheduling reference; exact links, inline directives, fallback targets, rule ordering, legacy compatibility | product-value, architecture, code-quality, business-continuity | French operator reference for time-aware links using inline `@schedule` directives beneath entries in `v8s-links.txt`. It documents the current exact-link-only scope, normal-target fallback, full and compact forms, timezone placement, ordered rule evaluation, and support for overnight windows with inclusive starts and exclusive ends. It also states that `custom/v8s-schedules.json` remains readable during the 3.x series for compatibility with existing instances and the current CLI, while newly hand-authored schedules belong inline. Splat links remain path-driven and outside scheduling support. | French counterpart; does not cover splat schedules or detailed use-case selection. |
| DOC-059 | `website-docs:content/docs/reference/security-model.en.md` | `not-retained` | Searchable Markdown (English) | Security-review reference; trust boundaries, enforcement layers, secrets, analytics data, supply chain, review order | security-privacy, architecture, business-continuity, revenue-risk, code-quality | English reference written specifically for security reviewers. It maps attacker- and operator-controlled inputs across build time, Worker runtime, and the Cloudflare edge, then assigns protocol, target, method, asset, authentication, scanner, rate, transport, and header controls to their enforcement layers. It also documents secrets handling, truncated visitor IP analytics, the absence of runtime npm dependencies, upgrade trust assumptions, protected instance files, and a staged review order. Its closing notes identify frequently reviewed areas and explicitly state that they are not known exploitable issues. | Provides the cross-layer model, not full Worker behavior, dashboard configuration procedures, or source-code analysis. |
| DOC-060 | `website-docs:content/docs/reference/security-model.fr.md` | `not-retained` | Searchable Markdown (French) | Security-review reference; trust boundaries, enforcement layers, secrets, analytics data, supply chain, review order | security-privacy, architecture, business-continuity, revenue-risk, code-quality | French reference written specifically for security reviewers. It maps attacker- and operator-controlled inputs across build time, Worker runtime, and the Cloudflare edge, then assigns protocol, target, method, asset, authentication, scanner, rate, transport, and header controls to their enforcement layers. It also documents secrets handling, truncated visitor IP analytics, the absence of runtime npm dependencies, upgrade trust assumptions, protected instance files, and a staged review order. Its closing notes identify frequently reviewed areas and explicitly state that they are not known exploitable issues. | French counterpart; provides the cross-layer model rather than full Worker behavior, dashboard procedures, or source-code analysis. |
| DOC-061 | `website-docs:content/docs/reference/timezones.en.md` | `not-retained` | Searchable Markdown (English) | Configuration reference; IANA timezones, UTC, daylight saving time, regional examples, runtime-supported list | product-value, code-quality, business-continuity | English reference for operators choosing timezone values during setup and link scheduling. It accepts IANA place-based names plus `UTC`, rejects numeric offsets as inadequate for daylight-saving transitions, and gives Eastern Time examples using Toronto and New York. A regional table lists common choices across Canada, the United States, Europe, Latin America, Africa, the Middle East, Asia, and Oceania. The page explains that the exact current list comes from the JavaScript runtime and provides a Node.js command for displaying locally supported values. | Common table is not exhaustive; exact acceptance depends on the JavaScript runtime used locally and by the product surfaces. |
| DOC-062 | `website-docs:content/docs/reference/timezones.fr.md` | `not-retained` | Searchable Markdown (French) | Configuration reference; IANA timezones, UTC, daylight saving time, regional examples, runtime-supported list | product-value, code-quality, business-continuity | French reference for operators choosing timezone values during setup and link scheduling. It accepts IANA place-based names plus `UTC`, rejects numeric offsets as inadequate for daylight-saving transitions, and gives Eastern Time examples using Toronto and New York. A regional table lists common choices across Canada, the United States, Europe, Latin America, Africa, the Middle East, Asia, and Oceania. The page explains that the exact current list comes from the JavaScript runtime and provides a Node.js command for displaying locally supported values. | French counterpart; common table is not exhaustive, and exact acceptance depends on the JavaScript runtime. |
| DOC-063 | `website-docs:content/docs/reference/upgrading.en.md` | `not-retained` | Searchable Markdown (English) | Maintenance procedure; upstream releases, protected instance files, validation, review and publishing | maintenance-cost, business-continuity, architecture, code-quality, security-privacy | English procedure for operators refreshing an existing vanityURLs instance with `npm run upgrade`. It covers the current clean-worktree workflow, retrieval of product-owned files, dependency installation when definitions change, project checks, and review through a normal Git diff. The default source is described as the latest stable release tag, with explicit examples for pinning `v3.3.1` or selecting `main` for testing. It lists instance-owned files and secrets that remain untouched and provides post-upgrade review, validation, staging, commit, and push commands. | Does not provide detailed rollback, merge-conflict resolution, or Cloudflare Pages migration steps; migration is linked separately. |
| DOC-064 | `website-docs:content/docs/reference/upgrading.fr.md` | `not-retained` | Searchable Markdown (French) | Maintenance procedure; upstream releases, protected instance files, validation, review and publishing | maintenance-cost, business-continuity, architecture, code-quality, security-privacy | French procedure for operators refreshing an existing vanityURLs instance with `npm run upgrade`. It covers the current clean-worktree workflow, retrieval of product-owned files, dependency installation when definitions change, project checks, and review through a normal Git diff. The default source is described as the latest stable release tag, with explicit examples for pinning `v3.3.1` or selecting `main` for testing. It lists instance-owned files and secrets that remain untouched and provides post-upgrade review, validation, staging, commit, and push commands. | French counterpart; does not provide detailed rollback, merge-conflict resolution, or Cloudflare Pages migration steps. |
| DOC-065 | `website-docs:content/docs/setup/_index.en.md` | `not-retained` | Searchable Markdown (English) | Setup overview; prerequisites, happy path, domain, GitHub, Cloudflare, workstation, secret handling | product-value, contributor-vendor-value, architecture, security-privacy, expense-exposure | English setup landing page for prospective and first-time vanityURLs operators. It presents a six-step happy path that can produce an instance in about ten minutes once prerequisites are available: select a short domain, use Cloudflare DNS, create a GitHub repository, configure locally, deploy the Worker, and test links. It inventories required accounts and tooling, including Cloudflare Access’s team domain, Git, Node.js 20 or newer, npm, and jq. It also directs operators to keep Cloudflare, analytics, identity-provider, OAuth, and recovery secrets outside Git. | Overview only; detailed installer questions, branching, account creation, Access configuration, and command execution live in linked guides. |
| DOC-066 | `website-docs:content/docs/setup/_index.fr.md` | `not-retained` | Searchable Markdown (French) | Setup overview; prerequisites, happy path, domain, GitHub, Cloudflare, workstation, secret handling | product-value, contributor-vendor-value, architecture, security-privacy, expense-exposure | French setup landing page for prospective and first-time vanityURLs operators. It presents a six-step happy path that can produce an instance in about ten minutes once prerequisites are available: select a short domain, use Cloudflare DNS, create a GitHub repository, configure locally, deploy the Worker, and test links. It inventories required accounts and tooling, including Cloudflare Access’s team domain, Git, Node.js 20 or newer, npm, and jq. It also directs operators to keep Cloudflare, analytics, identity-provider, OAuth, and recovery secrets outside Git. | French counterpart; overview only, with detailed installer questions, branching, account creation, Access configuration, and commands delegated to linked guides. |
| DOC-067 | `website-docs:content/docs/setup/flowcharts.en.md` | `not-retained` | Markdown (Hugo content page) | Setup flow reference; installer prompts, defaults, optional branches, generated files, verification | architecture, code-quality, security-privacy | Explains the interactive `npm run setup` sequence for operators and maintainers who want to understand or change installer question groups. It maps the first-setup happy path, core identity values, analytics choice, Access and localization, jurisdiction and contacts, optional branding, file writes, and final verification. Existing random-slug length is preserved and a missing value becomes `3`. Optional areas stay disabled on the happy path, while detailed analytics, jurisdiction, and manual public-page customization are delegated to linked pages. | Interactive installer map only; not a full configuration reference, legal guide, analytics-provider guide, or manual HTML procedure. |
| DOC-068 | `website-docs:content/docs/setup/flowcharts.fr.md` | `not-retained` | Markdown (Hugo content page) | French setup flow reference; installer prompts, defaults, optional branches, generated files, verification | architecture, code-quality, security-privacy | Presents the French-language map of the interactive `npm run setup` sequence for operators and maintainers reviewing the installer. It covers the first-configuration happy path, service identity, analytics, Cloudflare Access, languages and timezone, jurisdiction and public contacts, optional branding, instance-file generation, and verification. Existing `links.random_slug_length` values remain unchanged, while a missing value is written as `3`. Optional analytics, legal pages, and branding stay deferred on the happy path, with linked pages carrying later customization detail for operators. | Flowchart-oriented setup overview only; excludes full provider configuration, legal drafting, jurisdiction tuning, and detailed public-page editing. |
| DOC-069 | `website-docs:content/docs/setup/quickstart.en.md` | `not-retained` | Markdown (Hugo content page) | Operator quickstart; repository setup, stable detach, configuration, Cloudflare deployment, access protection, validation | product-value, project-health, security-privacy, architecture | Guides new instance operators from a workstation directory to a first Cloudflare deployment, using the v8s.link demo for concrete values. The phase-one workflow covers GitHub authentication, cloning, detaching to the latest stable release, tool and dependency installation, `npm run setup`, local helpers, initial commits, Workers & Pages connection, Cloudflare Access and network protection, validation, and deployed-path tests. It notes repeatable setup behavior and starter links. Analytics, jurisdiction and legal pages, and branding remain deferred, while unreleased-code testing is an explicit alternate path. | First-deployment path for a plain instance; not a complete customization, legal, analytics, or ongoing-operations manual. |
| DOC-070 | `website-docs:content/docs/setup/quickstart.fr.md` | `not-retained` | Markdown (Hugo content page) | French operator quickstart; repository setup, stable detach, configuration, Cloudflare deployment, access protection, validation | product-value, project-health, security-privacy, architecture | Guides French-reading instance operators from a local source directory through the first Cloudflare deployment, using v8s.link values as the working example. The phase-one sequence covers GitHub authentication, cloning, detaching to a stable release, required tools, dependencies, interactive setup, local helpers, Git publication, Workers & Pages connection, Access protection, network protection, validation, and deployment tests. It also describes repeatable setup and initial links. Analytics, complete legal and jurisdiction pages, and branding are left for later customization; unreleased code uses a separate detach option. | French first-deployment guide for a basic instance; excludes comprehensive customization and long-term operational procedures. |
| DOC-071 | `website-docs:content/docs/v8s-link/_index.en.md` | `not-retained` | Markdown (Hugo section page) | Demo landing page; troubleshooting navigation, configuration versus operations | product-value, project-health | Serves as the English landing page for the official v8s.link demo, aimed at people building or debugging their own vanityURLs instance. It divides troubleshooting into two child paths: configuration for differences in setup values or files, and operations for unexpected redirects, status pages, or link-state behavior. Its scope is navigational rather than procedural: it identifies the demo’s role and directs readers to the relevant detail page, without listing configuration fields, deployment steps, operational commands, or the demo’s link inventory. | Short section index only; substantive configuration and runtime content resides in the linked child pages. |
| DOC-072 | `website-docs:content/docs/v8s-link/_index.fr.md` | `not-retained` | Markdown (Hugo section page) | French demo landing page; troubleshooting navigation, configuration versus operations | product-value, project-health | Serves as the French-language landing page for the official v8s.link demo, for readers constructing or debugging their own vanityURLs instance. It separates the next step by symptom: configuration covers setup values or files that differ, while operations covers unexpected redirects, status pages, and link states. The page is a navigation entry rather than a procedure. It states the demo’s support purpose and links to the two detailed areas, without reproducing configuration inventories, deployment instructions, runtime commands, or individual link examples. | Brief section index; configuration and operational detail is intentionally delegated to two linked pages. |
| DOC-073 | `website-docs:content/docs/v8s-link/configuration.en.md` | `not-retained` | Markdown (Hugo content page) | Demo configuration reference; instance files, setup values, contacts, languages, branding, link inventory | architecture, project-health, security-privacy | Documents the public v8s.link demo configuration for operators comparing a personal instance with a concrete example. It identifies source and deployed locations, the instance-owned files to inspect, setup values for domain, Worker, Cloudflare Access, contacts, languages, branding and deferred legal features, plus the human-authored link inventory and related references. The page supports Quickstart with non-placeholder values and directs comparison toward `custom/` before `defaults/`. It explicitly omits analytics, final legal text, jurisdiction-specific privacy and terms pages, and finished branding. | Public demo snapshot and example inventory; values are to be replaced during personal-instance setup and excluded features require separate customization. |
| DOC-074 | `website-docs:content/docs/v8s-link/configuration.fr.md` | `not-retained` | Markdown (Hugo content page) | French demo configuration reference; instance files, setup values, contacts, languages, branding, link inventory | architecture, project-health, security-privacy | Documents in French the configuration used by the public v8s.link demo, for operators comparing their instance with a concrete setup. It identifies the source repository, deployed site, relevant custom files, Worker configuration, domain and Access values, public contacts, supported languages, branding state, deferred legal features, and the human-authored link inventory. It supplies the concrete values used by Quickstart and points readers toward instance-owned files before product defaults. Analytics, final legal copy, jurisdiction-specific privacy and terms pages, and completed branding are expressly absent. | Demo configuration and link inventory only; it does not provide the omitted customization or reusable values for another operator. |
| DOC-075 | `website-docs:content/docs/v8s-link/operations.en.md` | `not-retained` | Markdown (Hugo content page) | Demo operations reference; live paths, localization fallback, link states, namespaces, CLI and security references | architecture, project-health, security-privacy | Describes the deployed v8s.link demo for operators investigating runtime behavior. It identifies the source repository and live site, explains English fallback behavior, lists core public and lifecycle pages, states the source link format and local CLI commands for viewing or adding links, and gives representative exact, nested, lifecycle, documentation, and status shortcuts. It also points to references for link format, runtime security, access control, destination policy, monitoring, and edge protection. The scope is the demo’s current operational examples, not setup values or a complete operator runbook. | Demo runtime reference with selected paths and links; excludes configuration values, deployment procedure, and exhaustive operations coverage. |
| DOC-076 | `website-docs:content/docs/v8s-link/operations.fr.md` | `not-retained` | Markdown (Hugo content page) | French demo operations reference; live paths, localization fallback, link states, namespaces, CLI and security references | architecture, project-health, security-privacy | Describes in French the deployed v8s.link demo for operators examining runtime behavior. It names the repository and live instance, explains English as the main and fallback language, catalogs core and lifecycle pages, identifies the human-authored link source and CLI commands, and shows examples of nested namespaces, redirect states, true not-found behavior, and project shortcuts. Linked operational references cover link format, runtime security, Cloudflare Access, destination policy, monitoring, and edge protection. The page concerns current demo behavior rather than setup configuration or a full runbook. | Selected demo behavior and inventory only; configuration fields, deployment steps, and comprehensive incident procedures are outside scope. |
| DOC-077 | `docs/README.md` | `not-retained` | Markdown (repository guide) | Instance operator guide; ownership boundaries, link workflow, checks, upgrades, Cloudflare deployment, release reminders | project-health, maintenance-cost, security-privacy, architecture | Provides the operator-facing repository guide for a detached vanityURLs instance. It defines `custom/` and `wrangler.toml` as instance-owned sources, `defaults/` as upgrade-refreshed product files, and `build/`, `src/`, and `functions/` as generated output. It covers the everyday link-editing, checking, committing, and Cloudflare deployment workflow; important configuration files; grouped and focused commands; stable-release upgrades; and optional monthly or local upstream-release checks. It states that the product does not phone home and points to web documentation, while excluding product-maintainer release metadata. | Repository-level operating summary; detailed setup, CLI, configuration, and upgrade procedures remain on the linked documentation site. |
| DOC-078 | `docs/adr/0001-use-release-please-and-semantic-versioning.md` | `not-retained` | Markdown (ADR) | Release architecture decision; release-please, Conventional Commits, semantic versioning, signed tags, detached instances | architecture, project-health, maintenance-cost, security-privacy | Records the accepted 2026-05-15 release process for maintainers and instance owners who need predictable upgrade versions and release information. It assigns release pull requests, version bumps, changelog entries, and notes to release-please, driven by Conventional Commits and semantic versioning. Trusted maintainers separately create signed tags under ADR 0015, and detached instances remove upstream release metadata. The decision also situates formatting, build, lint, and test automation in npm scripts. It does not treat release-please as the final signer or make instance repositories product publishers. | Decision record, not the operational signing procedure; that workflow is delegated to `RELEASE_WORKFLOW.md` and ADR 0015. |
| DOC-079 | `docs/adr/0002-keep-instance-changes-in-custom.md` | `not-retained` | Markdown (ADR) | Ownership-boundary decision; product defaults, custom instance files, generated output, upgrade preservation | architecture, maintenance-cost, project-health | Records the accepted 2026-05-19 file-ownership boundary for maintainers and instance operators receiving product updates. Product-owned defaults and scripts remain under `defaults/` and `scripts/`; instance links, policies, branding, and operator settings belong under `custom/`; generated runtime artifacts belong under `build/` and `src/`. The build copies defaults, overlays custom content, and writes generated output. The decision defines `custom/` as the editable operational boundary and treats generated files as reproducible outputs. Direct edits to defaults or build output are outside normal instance operation unless contributing upstream. | High-level ownership contract; does not enumerate every upgrade path or describe individual configuration schemas. |
| DOC-080 | `docs/adr/0003-custom-policy-replaces-default-policy.md` | `not-retained` | Markdown (ADR) | Policy-source decision; replacement semantics, generated feeds, allow domains, migration compatibility | security-privacy, architecture, maintenance-cost | Records the accepted 2026-05-19 source-policy behavior for operators and maintainers controlling destination safety. When `custom/v8s-policies.json` exists, it replaces rather than merges with the default policy, preventing removed local entries from returning after upgrades. Runtime `build/v8s-blocklist.json` is generated from one source policy, with optional generated feed data added afterward and allow-domain entries able to override generated blocks. Empty or minimal custom policies remain valid, and legacy blocklist filenames retain migration compatibility. The decision does not make generated feeds part of the default-source merge. | Defines source selection and compatibility; excludes the full policy schema, feed configuration, and destination-safety operating procedure. |
| DOC-081 | `docs/adr/0004-detach-and-upgrade-product-files.md` | `not-retained` | Markdown (ADR) | Instance lifecycle decision; detach, upgrades, protected paths, dependencies, config inheritance, release trust | architecture, maintenance-cost, security-privacy, project-health | Records the accepted 2026-05-21 lifecycle for turning upstream code into an independent instance and later refreshing product files. `npm run detach` removes upstream metadata, history, maintainer guidance, and release files while installing the operator README. `npm run upgrade` refreshes product-owned paths but protects `custom/`, `wrangler.toml`, secrets, and the instance README. It also covers dependency installation before checks, deep-merging default and custom configuration, and signed-release trust for executable upgrades. Protected local files remain operator-owned, and upgrades do not restore the upstream README. | Product-file detach and upgrade contract; does not transfer responsibility for protected local paths or define the complete signing workflow. |
| DOC-082 | `docs/adr/0005-support-deferred-legal-pages.md` | `not-retained` | Markdown (ADR) | Setup and legal-page decision; phased launch, trust contacts, security.txt, deferred privacy and terms | security-privacy, product-value, business-continuity | Records the accepted 2026-05-22 phased setup behavior for new operators who need an initial redirector before completing operator-specific legal decisions. The `operator.legal_pages_enabled` setting allows privacy, terms, and standalone security pages to be deferred. During deferral, Trust & Safety remains deployed and `/.well-known/security.txt` is generated when contact data is valid. Full pages can be enabled by rerunning setup after required operator, jurisdiction, contact, and date fields are available. The decision preserves early abuse and vulnerability contact paths but does not supply legal content or review. | Product behavior for deferral, not legal advice or completed privacy, terms, jurisdiction, and standalone security-page text. |
| DOC-083 | `docs/adr/0006-use-readable-random-slugs.md` | `not-retained` | Markdown (ADR) | Link-generation decision; readable alphabet, default length, tag-specific lengths, backward-compatible schema fields | product-value, code-quality, architecture | Records the accepted 2026-05-26 random-slug generation behavior for CLI users and maintainers. Generated slugs draw from a readable alphabet configured in custom site settings or inherited from product defaults, avoiding visually ambiguous or awkward characters. The site configuration stores a base random length, alphabet, and tag-specific length map. When several tags provide lengths, `lnk` selects the shortest configured value. These fields are additive and do not trigger a stored `schema_version` change. The decision covers generated slugs only, not user-supplied slugs or broader link validation. | Limited to CLI-generated slug alphabet and length selection; excludes manual slug naming and unrelated routing rules. |
| DOC-084 | `docs/adr/0007-scope-scanner-keywords-to-requests.md` | `not-retained` | Markdown (ADR) | Security-policy decision; scanner probes, request and target scopes, valid long URLs, compatibility | security-privacy, code-quality, architecture | Records the accepted 2026-05-26 scope rule for policy authors and maintainers handling scanner-probe keywords. Terms associated with WordPress, PHP, and similar endpoint scans apply to request paths by default, because the same strings may be legitimate within redirect destinations. Policy entries can explicitly select request, target, or combined scope. Scanner probes remain blocked before short-link lookup, while valid long URLs such as destinations ending in `.php` can remain usable. Existing scanner policies stay compatible, and target blocking remains available when a keyword is specifically configured for it. | Keyword-scope decision only; does not define the complete destination policy, keyword catalog, or scanner-detection implementation. |
| DOC-085 | `docs/adr/0008-site-config-schema-versioning.md` | `not-retained` | Markdown (ADR) | Configuration-contract decision; schema-version triggers, additive fields, migrations, changelog and tests | architecture, maintenance-cost, code-quality | Records the accepted 2026-05-26 versioning contract for maintainers evolving default and custom site configuration. `schema_version` remains `1.0` for additive, backward-compatible optional fields that default merging can supply to existing instances. A version increment is reserved for incompatible field semantics or stored custom files requiring migration. Additive changes still belong in `docs/schema-changelog.md`. Any future version change should update default configuration, provide installer or upgrade handling, document the migration in release material or an ADR, and include practical tests or checks. Exact field definitions remain in source files. | Versioning policy rather than a complete schema; exact fields, validation, and individual migrations remain in source and related records. |
| DOC-086 | `docs/adr/0009-record-architecture-decisions.md` | `not-retained` | Markdown (ADR) | Documentation-governance decision; repository ADRs, numbering, structure, relationship to public documentation | architecture, project-health, maintenance-cost | Records the accepted 2026-05-26 documentation practice for maintainers explaining code-level contracts near their implementation. Architecture decisions are stored as numbered Markdown files under `docs/adr/`, using a compact title, date, status, context, decision, and consequences structure. The public website may link to these records when implementation detail matters, but remains focused on product use rather than becoming an internal design log. Repository ADRs travel with code and can accompany contract changes in the same contribution. They do not replace user documentation, tests, or inline comments. | Governance and file-structure decision only; it does not document individual architecture choices or replace other documentation forms. |
| DOC-087 | `docs/adr/0010-keep-product-starter-links-small.md` | `not-retained` | Markdown (ADR) | Starter-content decision; default link registry, setup generation, scheduled example, demo separation | product-value, maintenance-cost, code-quality | Records the accepted 2026-05-26 starter-registry scope for new instance operators and maintainers. `defaults/v8s-links.txt` contains only `home`, `contact`, and `docs`. When a custom link source is absent, setup copies this product source and adapts home and owner values for the instance. Inline schedule directives paired with `contact` provide one working scheduled-link example. Larger personal, namespace, lifecycle, and project inventories belong in demo instances or documentation. The decision removes a separate hard-coded starter list and requires starter schedules to reference slugs present in the starter registry. | Applies to initial product links and their schedule example; excludes larger demo inventories and operator-specific link design. |
| DOC-088 | `docs/adr/0011-use-prettier-and-check-gate.md` | `not-retained` | Markdown (ADR) | Quality-gate decision; Prettier, project linting, grouped commands, CI order, setup verification | code-quality, project-health, maintenance-cost | Records the accepted 2026-05-26 formatting and verification workflow for maintainers, CI, and instance operators. Prettier rewrites or checks supported text formats, while `scripts/lint.mjs` retains project-specific checks for trailing newlines, JSON parsing, JavaScript syntax, and Wrangler expectations. `npm run check` becomes the single local and CI gate, running format verification, build, lint, and tests in order. Predictable `test`, `validate`, `smoke`, and focused command variants remain available. The GitHub workflow runs `npm ci` before the gate, and setup can present its result concisely. | Defines the shared gate and command grouping; Prettier does not replace product-specific linting or runtime tests. |
| DOC-089 | `docs/adr/0012-maintain-cloudflare-dashboard-capture.md` | `not-retained` | Markdown (ADR) | Operations-documentation decision; Cloudflare UI capture, baselines, plan gates, reference-only settings | security-privacy, maintenance-cost, project-health | Records the accepted 2026-05-29 documentation practice for maintainers tracking Cloudflare dashboard settings that can change names, locations, defaults, quotas, or plan availability. A structured capture in the website repository serves as the comparison source for protection guidance, including required baseline settings and surfaces explicitly marked reference-only or unnecessary. Public documentation remains focused on the operator path, while non-baseline detail can move to reference pages. Future UI reviews compare capture data rather than relying solely on prose or screenshots. Product runtime changes still require separate architecture decisions. | Documentation baseline for Cloudflare surfaces; it does not make reference-only analytics, cache, redirect, or diagnostic settings setup requirements. |
| DOC-090 | `docs/adr/0013-runtime-registry-schema-3.md` | `not-retained` | Markdown (ADR) | Runtime-data decision; schema 3.1, tree registry, exact and splat links, flattened tooling views | architecture, scalability, code-quality | Records the accepted 2026-05-30 runtime registry contract for maintainers of the generator, Worker, validators, dashboard, and local tools. Human-edited links remain flat in `custom/v8s-links.txt`, while the build compiles `build/v8s.json` as schema `3.1` with a canonical nested `tree`. Nodes contain children and may hold exact or splat links; the former `links[]` runtime array is removed. Tools flatten the tree only when tabular output is needed. Exact and splat aliases may share a base slug, and rollback leaves the human source registry unchanged. | Generated runtime shape only; source link authoring stays flat, and tabular consumers must derive their own flattened view. |
| DOC-091 | `docs/adr/0014-prefer-repository-owned-configuration.md` | `not-retained` | Markdown (ADR) | Configuration-control decision; Git ownership, Cloudflare exceptions, reproducibility, headers and generated pages | architecture, security-privacy, business-continuity | Records the accepted 2026-06-04 control-plane boundary for operators and maintainers of redirects-as-code. Links, policies, generated pages, headers, Worker settings, and customizations should remain repository-owned when source can represent them. Cloudflare dashboard configuration is reserved for secrets, identifiers, certificates, DNS, Access, WAF, rate limits, bot controls, account features, and temporary incident response. It must not duplicate repository-owned CSP, HSTS, public files, redirects, or generated status pages. Necessary external settings require documentation, while mirrored dashboard settings remain disabled to preserve a single source of behavior. | Preference for source-backed behavior with named platform exceptions; it does not eliminate Cloudflare-only controls or emergency mitigations. |
| DOC-092 | `docs/adr/0015-require-signed-release-tags.md` | `not-retained` | Markdown (ADR) | Supply-chain decision; signed releases, pinned identities, tag protection, stable upgrades, updater bootstrap | security-privacy, business-continuity, project-health | Records the accepted 2026-06-04 release-provenance model for maintainers and operators whose upgrades may execute synchronized product code. Release-please prepares release changes, while a trusted maintainer signs `vX.Y.Z` tags with Sigstore/gitsign using pinned email identities and the GitHub OIDC issuer. Tag rules block deletion and mutation, and upgrades default to the newest stable release rather than `main`. The updater may bootstrap newer upgrade tooling from the same fetched source before synchronization. The included tag procedure covers configuration, signing, verification, and pushing; signatures establish authorization, not code safety. | Provenance and release-operations contract; it does not guarantee safe code, prevent compromise of trusted identities, or provide SLSA build guarantees. |
| DOC-093 | `docs/adr/0016-adopt-complexity-budget.md` | `not-retained` | Markdown (ADR) | Maintainability decision; ESLint, SonarJS, complexity budgets, reports, incremental functional-core refactoring | code-quality, maintenance-cost, project-health, scalability | Records the accepted 2026-06-04 complexity-management approach for maintainers of large operational modules. ESLint with a flat configuration and `eslint-plugin-sonarjs` joins the lint and check workflows, with concise and raw commands plus a complete report under `build/`. Initial warning-level budgets cover cyclomatic and cognitive complexity, file and function length, nesting, and parameter count. New work is expected to remain within the budget while existing violations are reduced during related changes. Refactoring separates pure transformations from filesystem, prompt, process, and network orchestration through incremental functional-core and imperative-shell decomposition. | Warning-level visibility and refactoring direction; current releases remain green, and budgets are not yet universal blocking errors. |
| DOC-094 | `docs/adr/0017-allow-first-party-splat-namespace-references.md` | `not-retained` | Markdown (ADR) | Redirect-validation decision; first-party namespaces, exact aliases, cycle detection, domain discovery | security-privacy, code-quality, architecture | Records the accepted 2026-06-08 validation behavior for operators reusing dynamic namespaces within their own short domain. Exact links may target a first-party splat route without warning, while an exact link targeting another exact local slug produces a warning and exact alias cycles fail validation. First-party domains come from site configuration and Cloudflare route declarations. These checks remain separate from public shortener-loop and platform-share target checks. Operators wanting two static aliases with one destination duplicate the long URL instead of chaining exact slugs. The rule preserves namespace reuse while exposing chains and blocking loops. | Applies to first-party exact and splat references; public third-party shortener detection remains a separate policy concern. |
| DOC-095 | `docs/adr/0018-keep-managed-public-assets-in-defaults.md` | `not-retained` | Markdown (ADR) | Public-asset ownership decision; product runtime assets, custom overlays, stale shadows, explicit forks | maintenance-cost, code-quality, architecture | Records the accepted 2026-06-15 ownership rule for operators and maintainers of public runtime assets. Product-managed `v8s-` CSS and JavaScript files remain in `defaults/public/`, allowing upgrades to supply current release assets without local copies shadowing them. `custom/public/` remains available for instance-owned pages, fonts, images, manifests, scripts, styles, and replacements using non-product filenames. Doctor identifies custom `v8s-*` shadows, and the asset-fix action removes them. Deliberate forks remain possible when documented through a narrow custom override, but then become locally maintained exceptions outside automatic product updates. | Distinguishes managed assets from instance-owned public content; deliberate `v8s-*` forks are allowed only as explicit local maintenance choices. |
| DOC-096 | `docs/adr/0019-opt-out-of-edge-html-transformation.md` | `not-retained` | Markdown (ADR) | Edge-security decision; strict CSP, `no-transform`, Cloudflare injection, public HTML baseline | security-privacy, architecture, code-quality | Records the accepted 2026-06-15 HTML delivery policy for operators and maintainers using strict Content Security Policy. Worker HTML responses append `no-transform`, and static fallback headers combine it with `no-store`, limiting intermediary markup or script injection. The operator baseline keeps challenge-style and page-rewriting Cloudflare features disabled on public redirect, lookup, and status pages rather than weakening CSP with inline allowances or generated hashes. Non-rewriting controls such as WAF, rate limits, Access, bot controls, and Browser Integrity Check remain available. Separate application surfaces may adopt their own injection-compatible policy. | Public vanityURLs HTML baseline only; challenge or rewriting features may still be used on separate applications or protected surfaces. |
| DOC-097 | `docs/schema-changelog.md` | `not-retained` | Markdown (schema changelog) | Configuration history; additive fields, compatibility, migrations, policies, local helpers, inline schedules | architecture, maintenance-cost, code-quality, security-privacy | Catalogs field-level configuration changes dated 2026-05-26 for maintainers and operators tracking stored contracts without relying only on `schema_version`. It covers operator-domain email defaults, random-slug settings, localized branding, deferred legal pages, expanded operator and analytics fields, workstation-local helper and publishing settings, the renamed policy source with replacement semantics, and inline schedule directives in the link source. Each section states compatibility and migration behavior. The changes are additive or rename-compatible without a version bump; incompatible stored-config changes remain governed by ADR 0008 and would require migration. | Changelog for listed field additions and compatibility behavior; exact schemas remain in source, and it does not record an incompatible version migration. |
